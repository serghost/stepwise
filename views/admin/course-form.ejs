<!DOCTYPE html>
<html lang="ru">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= course ? 'Редактирование курса' : 'Новый курс' %> | Stepwise</title>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="/css/style.css">
</head>
<body>
  <div class="admin-layout">
    <%- include('partials/sidebar', { active: 'courses' }) %>
    
    <main class="admin-main">
      <div class="breadcrumb">
        <a href="/admin/courses">Курсы</a>
        <span class="breadcrumb-sep">→</span>
        <span><%= course ? 'Редактирование' : 'Новый курс' %></span>
      </div>
      
      <div class="page-header">
        <h1 class="page-title"><%= course ? 'Редактирование курса' : 'Новый курс' %></h1>
      </div>
      
      <div class="card" style="max-width: 600px;">
        <form method="POST" action="<%= course ? '/admin/courses/' + course.id : '/admin/courses' %>">
          <div class="form-group">
            <label class="form-label">Название курса</label>
            <input type="text" name="title" class="form-input" required value="<%= course ? course.title : '' %>" placeholder="Введение в программирование">
          </div>
          
          <div class="form-group">
            <label class="form-label">Описание</label>
            <textarea name="description" class="form-textarea" placeholder="Краткое описание курса..."><%= course ? course.description || '' : '' %></textarea>
          </div>
          
          <div class="form-group">
            <label class="form-label">URL изображения (необязательно)</label>
            <input type="url" name="image_url" class="form-input" value="<%= course ? course.image_url || '' : '' %>" placeholder="https://example.com/image.jpg">
            <p class="form-help">Ссылка на картинку для обложки курса</p>
          </div>
          
          <div class="flex gap-2">
            <button type="submit" class="btn btn-primary">
              <%= course ? 'Сохранить' : 'Создать курс' %>
            </button>
            <a href="/admin/courses" class="btn btn-secondary">Отмена</a>
          </div>
        </form>
      </div>
    </main>
  </div>
</body>
</html>
