<!DOCTYPE html>
<html lang="ru">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Редактирование степа | Stepwise</title>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="/css/style.css">
</head>
<body>
  <div class="admin-layout">
    <%- include('partials/sidebar', { active: 'courses' }) %>
    
    <main class="admin-main">
      <div class="breadcrumb">
        <a href="/admin/courses">Курсы</a>
        <span class="breadcrumb-sep">→</span>
        <a href="/admin/courses/<%= course.id %>/steps"><%= course.title %></a>
        <span class="breadcrumb-sep">→</span>
        <span>Редактирование</span>
      </div>
      
      <div class="page-header">
        <h1 class="page-title">Редактирование степа</h1>
      </div>
      
      <div class="card" style="max-width: 700px;">
        <form method="POST" action="/admin/steps/<%= step.id %>" enctype="multipart/form-data">
          <div class="form-group">
            <label class="form-label">Название степа</label>
            <input type="text" name="title" class="form-input" required value="<%= step.title %>">
          </div>

          <div class="form-group">
            <label class="form-label">Тип степа</label>
            <div class="flex gap-2">
              <label class="radio-card">
                <input type="radio" name="step_type" value="info" <%= step.step_type === 'info' ? 'checked' : '' %> onchange="toggleAnswerConfig(this)">
                <span class="radio-card-content">
                  <span class="radio-card-icon">📖</span>
                  <span class="radio-card-title">Инфо</span>
                  <span class="radio-card-desc">Просто материал</span>
                </span>
              </label>
              <label class="radio-card">
                <input type="radio" name="step_type" value="task" <%= step.step_type !== 'info' ? 'checked' : '' %> onchange="toggleAnswerConfig(this)">
                <span class="radio-card-content">
                  <span class="radio-card-icon">✏️</span>
                  <span class="radio-card-title">Задание</span>
                  <span class="radio-card-desc">Требуется ответ</span>
                </span>
              </label>
            </div>
          </div>

          <div id="answerConfig" class="form-group" style="<%= step.step_type === 'info' ? 'display:none' : '' %>">
            <label class="form-label">Тип ответа</label>
            <div class="checkbox-group">
              <label class="checkbox-item">
                <input type="checkbox" name="answer_text" value="1" <%= (step.answer_type || '').includes('text') ? 'checked' : '' %>>
                <span>Текстовый ответ</span>
              </label>
              <label class="checkbox-item">
                <input type="checkbox" name="answer_file" value="1" <%= (!step.answer_type || step.answer_type.includes('file')) ? 'checked' : '' %>>
                <span>Файл / видео</span>
              </label>
            </div>
          </div>

          <div class="form-group">
            <label class="form-label">Содержимое / Задание</label>
            <textarea name="content" class="form-textarea" rows="10"><%= step.content || '' %></textarea>
            <p class="form-help">Опишите материал или задание.</p>
          </div>
          
          <% if (step.video_url) { %>
            <div class="form-group">
              <label class="form-label">Текущее видео</label>
              <div class="video-container" style="max-width: 400px;">
                <video controls style="width: 100%;">
                  <source src="<%= step.video_url %>" type="video/mp4">
                </video>
              </div>
              <label style="display: flex; align-items: center; gap: 0.5rem; margin-top: 0.75rem; cursor: pointer;">
                <input type="checkbox" name="remove_video" value="1">
                <span class="text-sm">Удалить видео</span>
              </label>
            </div>
          <% } %>
          
          <div class="form-group">
            <label class="form-label"><%= step.video_url ? 'Заменить видео' : 'Добавить видео' %></label>
            <input type="file" name="video" class="form-input" accept=".mp4,.mov,.webm,.avi">
            <p class="form-help">MP4, MOV, WebM — до 500 МБ</p>
          </div>
          
          <div class="flex gap-2">
            <button type="submit" class="btn btn-primary">Сохранить</button>
            <a href="/admin/courses/<%= course.id %>/steps" class="btn btn-secondary">Отмена</a>
          </div>
        </form>
      </div>
    </main>
  </div>
<script>
function toggleAnswerConfig(radio) {
  const config = document.getElementById('answerConfig');
  config.style.display = radio.value === 'task' ? 'block' : 'none';
}
</script>
</body>
</html>
